import { storiesOf } from '@storybook/react';
import React from 'react';
import MobileNavigationBar from './MobileNavigationBar';

storiesOf('MobileNavigationBar', module).add('Default', () => <MobileNavigationBar />);
