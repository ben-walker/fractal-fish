syntax = "proto3";

package go.micro.srv.auth;

service Auth {
	rpc Check(Request) returns (Response) {}
}

message Request {
	string credential = 1;
	string password = 2;
}

message Response {
	string msg = 1;
}
