import { storiesOf } from '@storybook/react';
import React from 'react';
import DesktopNavigationBar from './DesktopNavigationBar';

storiesOf('DesktopNavigationBar', module).add('Default', () => <DesktopNavigationBar />);
